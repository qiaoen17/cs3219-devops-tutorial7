apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: cs3219
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service # match with labels.app
  template:
    metadata:
      labels:
        app: order-service # match with matchLabels.app
    spec:
      containers:
        - name: order-service
          image: qiaoen17/order-service:1.0.0 
          imagePullPolicy: IfNotPresent
          ports:
          - containerPort: 8080